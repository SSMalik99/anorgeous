import React from "react";

const Footer = () => {
    return (
        <footer className="text-center p-2 fixed-bottom bg-dark text-white">
            @Anorgeous {new Date().getFullYear()}
        </footer>
    )
}

export default Footer